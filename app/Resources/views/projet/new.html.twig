{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Projet creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}


            <a href="{{ path('projet_index', { 'id': app.user.id })  }}">Retour aux Projets</a>

{% endblock %}
