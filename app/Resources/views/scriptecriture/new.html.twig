{% extends 'layout.html.twig' %}

{% block content %}

    {{ dump(script) }}

    <h1>Scriptecriture creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('scriptecriture_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
