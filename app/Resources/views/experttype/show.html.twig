{% extends 'base.html.twig' %}

{% block body %}
    <h1>Experttype</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expertType.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ expertType.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('experttype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('experttype_edit', { 'id': expertType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
